/**
 * Versioned paths, each confirmed against the current Partner Center reference
 * page. Generation numbers move; a 404 from any of these means the generation
 * changed and the path must be re-confirmed, not guessed.
 */

/** partner.tiktokshop.com/docv2/page/get-creator-profile-202405 — note: plural. */
export const CREATOR_PROFILE_PATH = "/affiliate_creator/202405/profiles";
